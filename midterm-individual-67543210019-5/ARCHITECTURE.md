C1: Context
┌─────────────────────────────────────────────┐
│              Staff / System Users           │
│  (Developer, Librarian, Admin/Manager)      │
└───────────────┬─────────────────────────────┘
                │ HTTP/JSON (CRUD, Reports)
                ▼
┌─────────────────────────────────────────────┐
│       Library Management System (LMS)       │
│  • จัดการข้อมูลหนังสือ (CRUD)                    │
│  • บันทึกการยืม-คืน                             │
│  • ตรวจสอบสถานะหนังสือ                        │
│  • แสดงรายงานสถิติ                            │
└───────────────┬─────────────────────────────┘
                │ SQL Queries
                ▼
┌─────────────────────────────────────────────┐
│              SQLite Database                │
│           (books.db / users.db)             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│             External Users / Members        │
│                   (ผู้ใช้บริการ)                │
└─────────────────────────────────────────────┘
                │ HTTP/JSON (Search/Borrow)
                ▼
┌─────────────────────────────────────────────┐
│       Library Management System (LMS)       │
└─────────────────────────────────────────────┘
ภาพรวมระบบ

Library Management System (LMS) เป็นระบบศูนย์กลางสำหรับจัดการข้อมูลหนังสือและการยืม–คืน
ให้บริการผ่าน HTTP/JSON API โดยมีผู้ใช้งานหลายกลุ่ม

Actors (ผู้ใช้งาน)
1️⃣ Staff / System Users

เช่น:

Developer

Librarian

Admin / Manager

ความสามารถ

จัดการข้อมูลหนังสือ (CRUD)

บันทึกการยืม–คืน

ตรวจสอบสถานะหนังสือ

ดูรายงานและสถิติ

การสื่อสาร

ใช้ HTTP/JSON เรียก API ของ LMS
รับผลลัพธ์เป็น JSON Response

2️⃣ External Users / Members (ผู้ใช้บริการ)

เช่น:

สมาชิกห้องสมุด

ผู้ใช้ทั่วไป

ความสามารถ

ค้นหาหนังสือ

ยืม / คืนหนังสือ

การสื่อสาร

ใช้ HTTP/JSON ผ่าน Web / Mobile App

C2: Contrainer Diagram แสดงโครงสร้าง Layered Architecture
┌───────────────────────────────┐
│           CLIENT              │
│ (Browser / Mobile App / API) │
└─────────────┬─────────────────┘
              │ HTTP/JSON (Search, Borrow, CRUD)
              ▼
╔══════════════════════════════════════════════╗
║       LIBRARY MANAGEMENT SYSTEM (LMS)       ║
║           (Express.js Application)          ║
╠══════════════════════════════════════════════╣
║  ┌────────────────────────────────────────┐ ║
║  │ 📋 PRESENTATION LAYER                   │ ║
║  │ • Routes (bookRoutes.js)               │ ║
║  │ • Controllers (bookController.js)      │ ║
║  │ • Middlewares (errorHandler.js)        │ ║
║  └───────────────┬────────────────────────┘ ║
║                  │ JS Objects: {title, author, isbn, stock} ║
║                  ▼                                        ║
║  ┌────────────────────────────────────────┐ ║
║  │ 🧠 BUSINESS LOGIC LAYER                 │ ║
║  │ • Services (bookService.js)            │ ║
║  │ • Validators (bookValidator.js)        │ ║
║  │ Business Rules:                         │ ║
║  │   ✓ Title, author, ISBN required        │ ║
║  │   ✓ Stock >= 0                          │ ║
║  │   ✓ Calculate available copies          │ ║
║  └───────────────┬────────────────────────┘ ║
║                  │ Data Objects: {id, title, author, isbn, stock} ║
║                  ▼                                        ║
║  ┌────────────────────────────────────────┐ ║
║  │ 💾 DATA ACCESS LAYER                    │ ║
║  │ • Repositories (bookRepository.js)     │ ║
║  │ • Database Connection (connection.js)  │ ║
║  │ Methods:                                │ ║
║  │   • findAll(category)                   │ ║
║  │   • findById(id)                        │ ║
║  │   • create(bookData)                    │ ║
║  │   • update(id, bookData)                │ ║
║  │   • delete(id)                          │ ║
║  └───────────────┬────────────────────────┘ ║
╚═════════════════╪════════════════════════╝
                  │ SQL Queries: SELECT, INSERT, UPDATE, DELETE
                  ▼
          ┌──────────────────────┐
          │   SQLite Database    │
          │    (library.db)      │
          │ Tables: books, users │
          └──────────────────────┘
C2 แสดงโครงสร้าง ภายใน LMS
โดยใช้ Layered Architecture (3-tier)

Container หลัก
🧑‍💻 Client

Browser

Mobile App

API Client (Postman / Thunder Client)

หน้าที่

ส่ง Request

รับ Response

ไม่มี Business Logic

🖥️ Library Management System (Express.js)

ระบบ Backend แบ่งออกเป็น 3 Layers ชัดเจน

1️⃣ Presentation Layer

ไฟล์หลัก

bookRoutes.js

bookController.js

errorHandler.js

หน้าที่

รับ HTTP Request

แปลง Request → Data Object

ส่ง Response กลับ Client

จัดการ Error

สิ่งที่ “ไม่ควรมี”

SQL

Business Rules

📌 เหตุผล

แยกการจัดการ HTTP ออกจาก Logic เพื่อให้ง่ายต่อการเปลี่ยน UI หรือ API Format

2️⃣ Business Logic Layer

ไฟล์หลัก

bookService.js

bookValidator.js

หน้าที่

ตรวจสอบความถูกต้องของข้อมูล

บังคับใช้ Business Rules เช่น:

ต้องมี title / author / ISBN

หนังสือที่ถูกยืมแล้ว ห้ามยืมซ้ำ

คำนวณสถิติหนังสือ

ตัวอย่าง Business Rules

ISBN ต้องถูก format

Stock ต้องไม่ติดลบ

Borrow ได้เฉพาะหนังสือที่ available

📌 เหตุผล

เป็นหัวใจของระบบ เปลี่ยน Database หรือ Framework ได้โดยไม่กระทบ Logic

3️⃣ Data Access Layer

ไฟล์หลัก

bookRepository.js

connection.js

หน้าที่

ติดต่อ Database

เขียน SQL Query

คืนข้อมูลเป็น Object

Methods

findAll()

findById()

create()

update()

delete()

📌 เหตุผล

แยก SQL ออกมาเพื่อ:

เปลี่ยน SQLite → MySQL / PostgreSQL ได้ง่าย

Mock Database สำหรับ Testing

🗄️ SQLite Database

จัดเก็บข้อมูลถาวร

Tables:

books

users

PART 3: Responsibilities ของแต่ละ Layer
1️⃣ Presentation Layer (การนำเสนอ)
🎯 หน้าที่

รับ HTTP Request จาก Client (Browser / Mobile / API)

แปลง Request ให้เป็นข้อมูลที่ Business Logic Layer เข้าใจ

เรียกใช้ Business Layer

แปลงผลลัพธ์เป็น HTTP Response (JSON)

จัดการ Error และส่ง Error Response กลับ Client

📁 ไฟล์ที่เกี่ยวข้อง
bookRoutes.js – กำหนด API Endpoints
bookController.js – จัดการ HTTP Request / Response
errorHandler.js – จัดการ Error แบบรวมศูนย์

2️⃣ Business Logic Layer (ตัวตนของระบบ)
🎯 หน้าที่

ตรวจสอบความถูกต้องของข้อมูล (Validation)

บังคับใช้ Business Rules

คำนวณและประมวลผลข้อมูล

เรียกใช้ Data Access Layer เมื่อต้องการข้อมูล

📁 ไฟล์ที่เกี่ยวข้อง
bookService.js – Business Logic หลัก
bookValidator.js – Validation Rules

Business Rules สำคัญ
✅ Required fields: title, author, isbn
function validateBookData(data) {
    if (!data.title || !data.author || !data.isbn) {
        throw new Error('Title, author, and ISBN are required');
    }
}

✅ Stock ต้องไม่ติดลบ
function validateStock(stock) {
    if (stock < 0) {
        throw new Error('Stock cannot be negative');
    }
}

✅ คำนวณจำนวนหนังสือทั้งหมด
const totalStock = books.reduce(
    (sum, book) => sum + book.stock,
    0
);

3️⃣ Data Access Layer (ประตูสู่ฐานข้อมูล)
🎯 หน้าที่

เชื่อมต่อกับ Database

ทำ CRUD Operations (Create, Read, Update, Delete)

แปลงผลลัพธ์จาก SQL เป็น JavaScript Objects

จัดการ Database Errors

📁 ไฟล์ที่เกี่ยวข้อง
bookRepository.js – CRUD Operations
connection.js – Database Connection

PART 4: Data Flow (Request → Response)
Example: Create Product

┌──────────────────────────────────────────────────────────────┐
│ 1. CLIENT                                                    │
│    POST /api/products                                       │
│    Body:                                                    │
│    {                                                        │
│      "name": "Laptop",                                      │
│      "price": 25000,                                        │
│      "stock": 10,                                           │
│      "category": "Electronics"                              │
│    }                                                        │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. PRESENTATION LAYER                                        │
│                                                              │
│ Routes (productRoutes.js)                                    │
│ └─ router.post('/', createProduct)                           │
│                                                              │
│ Controller (productController.js)                            │
│ ├─ Parse req.body → productData                              │
│ └─ Call productService.createProduct(productData)            │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 3. BUSINESS LOGIC LAYER                                      │
│                                                              │
│ Service (productService.js)                                  │
│ ├─ validateProductData()                                     │
│ │   • name, price, category required                         │
│ ├─ validatePrice() → price > 0                               │
│ └─ Call productRepository.create(productData)                │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 4. DATA ACCESS LAYER                                         │
│                                                              │
│ Repository (productRepository.js)                             │
│ ├─ Execute SQL INSERT                                        │
│ │   INSERT INTO products (...) VALUES (...)                  │
│ └─ Return created product object                              │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 5. DATABASE                                                  │
│    SQLite (products.db)                                      │
│    ├─ Insert record                                          │
│    └─ Return last inserted ID                                │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ (Response flows back up)
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 6. BUSINESS LOGIC LAYER                                      │
│    Return created product                                    │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 7. PRESENTATION LAYER                                        │
│    res.status(201).json(product)                              │
└────────────┬─────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│ 8. CLIENT                                                    │
│    Response: 201 Created                                     │
│    {                                                         │
│      "id": 1,                                                │
│      "name": "Laptop",                                       │
│      "price": 25000,                                         │
│      "stock": 10,                                            │
│      "category": "Electronics"                               │
│    }                                                         │
└──────────────────────────────────────────────────────────────┘
สรุป Data Flow
Client ส่ง Request → Controller รับและ parse →
Service ตรวจ validation และ business rules →
Repository เขียน SQL → Database →
Response ถูกส่งย้อนกลับตาม Layer เดิม